import * as SecureStore from 'expo-secure-store';import * as LocalAuthentication from 'expo-local-authentication';const KEY='networkos.app-lock';export type LockMode='off'|'biometric';export const SecurityService={getMode:async():Promise<LockMode>=>((await SecureStore.getItemAsync(KEY)) as LockMode)||'off',setMode:async(mode:LockMode)=>{await SecureStore.setItemAsync(KEY,mode)},enableBiometric:async()=>{const available=await LocalAuthentication.hasHardwareAsync()&&await LocalAuthentication.isEnrolledAsync();if(!available)return false;const result=await LocalAuthentication.authenticateAsync({promptMessage:'Enable NetworkOS app lock'});if(result.success)await SecurityService.setMode('biometric');return result.success}};
