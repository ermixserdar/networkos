import {getDatabase} from '@/database/database';import {now,uuid} from '@/types';export type Owner={id:string;first_name:string;last_name:string;email?:string|null;phone?:string|null;company?:string|null;job_title?:string|null};export const OwnerRepository={get:async()=>{const d=await getDatabase();return d.getFirstAsync<Owner>('SELECT * FROM owner_profile LIMIT 1')},save:async(data:Omit<Owner,'id'> & {id?:string})=>{const d=await getDatabase(),existing=await OwnerRepository.get(),id=data.id??existing?.id??uuid(),createdAt=existing?Date.now():now();await d.runAsync('INSERT INTO owner_profile(id,first_name,last_name,email,phone,company,job_title,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET first_name=excluded.first_name,last_name=excluded.last_name,email=excluded.email,phone=excluded.phone,company=excluded.company,job_title=excluded.job_title,updated_at=excluded.updated_at',id,data.first_name,data.last_name,data.email??null,data.phone??null,data.company??null,data.job_title??null,createdAt,now());return id}};
