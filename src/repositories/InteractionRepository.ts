import {getDatabase} from '@/database/database';import {Interaction,now,uuid} from '@/types';export const InteractionRepository={listForContact:async(contactId:string)=>{const d=await getDatabase();return d.getAllAsync<Interaction>('SELECT * FROM interactions WHERE contact_id=? AND deleted_at IS NULL ORDER BY interaction_at DESC',contactId)},create:async(data:Pick<Interaction,'contact_id'|'type'|'interaction_at'> & {title?:string;description?:string})=>{const d=await getDatabase(),id=uuid(),t=now();await d.runAsync('INSERT INTO interactions(id,contact_id,type,title,description,interaction_at,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?)',id,data.contact_id,data.type,data.title??null,data.description??null,data.interaction_at,t,t);await d.runAsync('UPDATE contacts SET last_contact_at=CASE WHEN last_contact_at IS NULL OR last_contact_at<? THEN ? ELSE last_contact_at END,updated_at=? WHERE id=?',data.interaction_at,data.interaction_at,t,data.contact_id);return id;},remove:async(id:string)=>{const d=await getDatabase();await d.runAsync('UPDATE interactions SET deleted_at=?,updated_at=? WHERE id=?',now(),now(),id)}};
