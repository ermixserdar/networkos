import {getDatabase} from '@/database/database';import {Relationship,now,uuid} from '@/types';export const RelationshipRepository={listForContact:async(id:string)=>{const d=await getDatabase();return d.getAllAsync<Relationship>('SELECT * FROM relationships WHERE (contact_a_id=? OR contact_b_id=?) AND deleted_at IS NULL ORDER BY updated_at DESC',id,id)},create:async(a:string,b:string,type:string,strength:number)=>{if(a===b)throw Error('A person cannot be connected to themselves');const d=await getDatabase(),[x,y]=[a,b].sort(),t=now();const existing=await d.getFirstAsync<Relationship>('SELECT id FROM relationships WHERE min(contact_a_id,contact_b_id)=? AND max(contact_a_id,contact_b_id)=? AND deleted_at IS NULL',x,y);if(existing){await d.runAsync('UPDATE relationships SET relationship_type=?,strength=?,updated_at=?,version=version+1 WHERE id=?',type,strength,t,existing.id);return existing.id}const id=uuid();await d.runAsync('INSERT INTO relationships(id,contact_a_id,contact_b_id,relationship_type,strength,created_at,updated_at) VALUES(?,?,?,?,?,?,?)',id,x,y,type,strength,t,t);return id},remove:async(id:string)=>{const d=await getDatabase();await d.runAsync('UPDATE relationships SET deleted_at=?,updated_at=? WHERE id=?',now(),now(),id)}};
