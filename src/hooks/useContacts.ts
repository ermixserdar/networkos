import {useCallback,useEffect,useRef,useState} from 'react';import {Contact} from '@/types';import {ContactRepository} from '@/repositories/ContactRepository';export function useContacts(query='',limit=100){const [contacts,setContacts]=useState<Contact[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState<string|null>(null);const timer=useRef<ReturnType<typeof setTimeout>|null>(null);const refresh=useCallback(async()=>{setLoading(true);setError(null);try{setContacts(await ContactRepository.list(query.trim(),limit,0));}catch{setError('load-failed');}finally{setLoading(false);}},[query,limit]);useEffect(()=>{if(timer.current)clearTimeout(timer.current);timer.current=setTimeout(()=>{void refresh();},query?250:0);return()=>{if(timer.current)clearTimeout(timer.current);};},[refresh,query]);return {contacts,refresh,loading,error};}
