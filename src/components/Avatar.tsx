import {Text,View} from 'react-native';export function Avatar({name,size=48}:{name:string;size?:number}){const initials=name.split(' ').map(x=>x[0]).join('').slice(0,2).toUpperCase();return <View style={{width:size,height:size,borderRadius:size/2,backgroundColor:'#DCE8E7',alignItems:'center',justifyContent:'center'}}><Text style={{color:'#173D42',fontWeight:'800',fontSize:size*.32}}>{initials}</Text></View>}
